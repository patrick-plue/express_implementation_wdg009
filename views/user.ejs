<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>Document</title>
  </head>
  <body>
    <% if (users) { %>
    <h1>Users</h1>
    <ul>
      <% users.forEach(function(users) { %>
      <li>
        <strong><%= users %></strong>
        <%= users %>
      </li>
      <% }); %>
    </ul>
    <% } %>
  </body>
</html>
